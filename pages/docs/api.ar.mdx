import { Callout } from 'nextra-theme-docs'

# API

机翻

```js
const { data, error, isLoading, isValidating, mutate } = useSWR(key, fetcher, options)
```

## المعلمات  رقم [#المعلمات]

 - `key`: سلسلة مفاتيح فريدة للطلب (أو وظيفة / صفيف / فارغة) [(التفاصيل)](/docs/arguments)، [(الاستخدام المتقدم)](/docs/conditional-fetching) رقم 
 - `fetcher`: (_optional_) وعد بإرجاع وظيفة لجلب بياناتك [(التفاصيل)](/docs/data-fetching) رقم 
 - `options`: (_optional_) كائن من الخيارات لخطاف SWR هذا رقم 
 
 ## قيم الإرجاع  رقم [#قيم العودة]

 - `data`: تم حل بيانات المفتاح المحدد بواسطة `fetcher` (أو غير محددة إذا لم يتم تحميلها) 
 - `error`: خطأ يلقيه `fetcher` (أو غير محدد) 
 - `isLoading`: إذا كان هناك طلب مستمر ولا يوجد "بيانات محملة". لا يتم النظر في البيانات الاحتياطية والبيانات السابقة
 - `isValidating`: إذا كان هناك طلب أو إعادة التحقق من الصحة رقم 
 - `mutate(data?, options?)`: وظيفة لتحوير البيانات المخزنة مؤقتا [(التفاصيل)](/docs/mutation) رقم 
 
 يمكن العثور على مزيد من المعلومات [هنا](/docs/advanced/understanding).

## الخيارات [#options] 

 - `suspense = false`: تمكين وضع React Suspense [(details)](/docs/suspense) 
 - `fetcher(args)`: وظيفة fetcher 
 - `revalidateIfStale = true`: إعادة التحقق تلقائيا حتى لو كانت هناك بيانات قديمة [(details)](/docs/revalidation#disable-automatic-revalidations) 
 - `revalidateOnMount`: تمكين أو تعطيل إعادة التحقق التلقائي عند تركيب المكون [(details)](/docs/revalidation#revalidate-on-mount) 
 - `revalidateOnFocus = true`: إعادة التحقق تلقائيا عندما يتم تركيز النافذة [(details)](/docs/revalidation) 
 - `revalidateOnReconnect = true`: إعادة التحقق تلقائيا عندما يستعيد المتصفح اتصال الشبكة (عبر `navigator.onLine22`) [(details)](/docs/revalidation) 
 - `refreshInterval` [(details)](/docs/revalidation): 
   - تم تعطيله افتراضيا: `refreshInterval = 0` 
   - إذا تم تعيينه على رقم، فاصل الاقتراع بالمللي ثانية 
   - إذا تم تعيينه على وظيفة، فستتلقى الدالة أحدث البيانات ويجب أن تعيد الفاصل الزمني بالمللي ثانية 
 - `refreshWhenHidden = false`: الاقتراع عندما تكون النافذة غير مرئية (إذا تم تمكين `refreshInterval`) 
 - `refreshWhenOffline = false`: الاقتراع عندما يكون المتصفح غير متصل بالإنترنت (يحدده `navigator.onLine`) 
 - `shouldRetryOnError = true`: أعد المحاولة عندما يكون لدى fetcher خطأ 
 - `dedupingInterval = 2000`: طلبات dedupe بنفس المفتاح في هذه الفترة الزمنية بالمللي ثانية 
 - `focusThrottleInterval = 5000`: إعادة التحقق مرة واحدة فقط خلال فترة زمنية بالمللي ثانية 
 - `loadingTimeout = 3000`: مهلة لتشغيل حدث onLoadingSlow بالمللي ثانية 
 - `errorRetryInterval = 5000`: فاصل إعادة المحاولة للخطأ بالمللي ثانية 
 - `errorRetryCount`: الحد الأقصى لعدد إعادة المحاولة للخطأ 
 - `fallback`: كائن ذو قيمة رئيسية لبيانات احتياطية متعددة [(مثال)](/docs/with-nextjs) 
 - `fallbackData`: يجب إرجاع البيانات الأولية (ملاحظة: هذا لكل خطاف) 
 - `keepPreviousData = false`: قم بإرجاع بيانات المفتاح السابق حتى يتم تحميل البيانات الجديدة [(details)](/docs/advanced/understanding#return-previous-data-for-better-ux) 
 - `onLoadingSlow(key, config)`: وظيفة رد الاتصال عندما يستغرق تحميل الطلب وقتا طويلا جدا (انظر `loadingTimeout22`) 
 - `onSuccess(data, key, config)`: وظيفة رد الاتصال عند انتهاء الطلب بنجاح 
 - `onError(err, key, config)`: وظيفة رد الاتصال عندما يقوم الطلب بإرجاع خطأ 
 - `onErrorRetry(err, key, config, revalidate, revalidateOps)`: معالج لإعادة محاولة الخطأ 
 - `onDiscarded(key)`: وظيفة معاودة الاتصال عند تجاهل الطلب بسبب ظروف السباق 
 - `قارن (أ، ب) `: وظيفة المقارنة المستخدمة للكشف عن وقت تغير البيانات المرتجعة، لتجنب عمليات إعادة التقديم الزائفة. بشكل افتراضي، يتم استخدام [stable-hash](https://github.com/shuding/stable-hash). 
 - `isPaused()`: وظيفة للكشف عما إذا كانت عمليات إعادة التحقق من الهوية مؤقتا، ستتجاهل البيانات والأخطاء التي تم جلبها عند إرجاع `true`. إرجاع "خطأ" افتراضيا. 
 - `use22`: مجموعة من وظائف البرامج الوسيطة [(details)](/docs/middleware)


<Callout emoji="💡">
  عندما تكون تحت شبكة بطيئة (2G، {'<='} 70 كيلوبت في الثانية)،<code>خطأ إعادة المحاولة الفاصل الزمني</code> سيكون 10s، و{''}
  <code>تحميل المهلة</code>سيكون 5s افتراضيا.
</Callout>

يمكنك أيضا استخدام [التكوين العالمي](/docs/global-configuration) لتوفير خيارات افتراضية.
